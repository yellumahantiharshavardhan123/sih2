{
  "name": "tripsafe-monorepo",
  "private": true,
  "workspaces": [
    "apps/admin",
    "packages/shared"
  ],
  "packageManager": "npm@10.5.0",
  "scripts": {
    "build": "npm -w apps/admin run build && npm -w packages/shared run build",
    "dev": "npm -w apps/admin run dev"
  }
}